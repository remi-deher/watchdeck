target-version = "py312"
line-length = 120

[lint]
select = ["E", "F", "W", "I"]
ignore = [
  "E501",  # line too long (géré par le formateur)
  "F401",  # unused import (faux positifs sur les __init__ et routers)
]
exclude = [
  "alembic/",
  ".venv/",
]
